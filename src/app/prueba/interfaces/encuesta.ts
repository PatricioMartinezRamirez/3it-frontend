export interface Resultado {
    id: number;
    email: string;
    genero?: number;
}

export interface Formulario{
    email: string;
    genero: string;
}

export interface Genero{
    nombre: string;
}
